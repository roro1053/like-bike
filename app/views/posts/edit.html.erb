<div class="edit-content">edit-content
<div class="edit-form"><%= link_to "HOME", root_path %>
<%= form_with model: @post, local: true do |f| %>
 <%= render 'shared/error_messages', model: f.object %>


<div class="text-box">
<%= f.text_area :text, class: "text-content" %>
</div>

<div class="click-upload">
<p>
クリックしてファイルをアップロード
</p>
 <%= f.file_field :image, class: "item-image" %>
</div>
 <%= f.submit "変更する" ,class:"update-btn" %>
 <div class="back-home"><%= link_to 'もどる', root_path, class:"back-btn" %></div>
  <% end %>
</div>
</div>
